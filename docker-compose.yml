services:
  nest:
    build: .
    ports:
      - 3000:3000
    env_file:
      - .env
    depends_on:
      - mysql

  mysql:
    image: mysql
    environment:
      - MYSQL_ALLOW_EMPTY_PASSWORD=yes
      - MYSQL_DATABASE=db
    volumes:
      - db-volume:/var/lib/mysql

volumes:
  db-volume:
